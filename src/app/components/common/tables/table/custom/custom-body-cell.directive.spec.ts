import { CustomBodyCellDirective } from './custom-body-cell.directive';

describe('CustomBodyCellDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomBodyCellDirective();
    expect(directive).toBeTruthy();
  });
});
