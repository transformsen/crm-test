import { TableRowDataPipe } from './table-row-data.pipe';

describe('TableRowDataPipe', () => {
  it('create an instance', () => {
    const pipe = new TableRowDataPipe();
    expect(pipe).toBeTruthy();
  });
});
